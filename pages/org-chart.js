import React from "react";
import { ScrollView, Text } from "react-native";
const RootOrgChart = () => {
  return (
    <ScrollView>
      <Text>test org chart</Text>
    </ScrollView>
  );
};

export default RootOrgChart;
